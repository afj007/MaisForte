ktor {
    deployment {
        port = 8081
        host = "0.0.0.0"
    }
    application {
        modules = [ com.maisforte.auth.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:postgresql://localhost:5432/maisforte"
    driver = "org.postgresql.Driver"
    user = "postgres"
    password = "postgres"
}
